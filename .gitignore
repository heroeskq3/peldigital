# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Editores / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code / asistentes
.claude/

# Logs y temporales
*.log
*.tmp
tmp/
cache/

# PHP / dependencias
/vendor/
composer.lock

# Configuración local sensible (si en el futuro se externaliza)
.env
.env.local
config.local.php

# Archivos crudos grandes (raw data) — se ignoran del repo pero deben existir en el servidor
# La estructura de carpetas se conserva via .gitkeep en cada subcarpeta
raw/padron/*.txt
raw/padron/*.zip
raw/avr/*.json
raw/geo/*.geojson
raw/geo/*.zip
!raw/**/.gitkeep

# Cache y logs generados automáticamente
data/poblacion_cache.json
data/bitacora.log
